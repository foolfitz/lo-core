# LibreOffice 26.2 Markdown 支援功能：架構分析

## 一、功能概述

LibreOffice 26.2 在 Writer（文書處理器）中新增了 Markdown 格式的匯入與匯出功能。使用者可以直接在 Writer 中開啟 `.md` 檔案進行編輯，也能將 Writer 文件另存為 Markdown 格式。這項功能讓 LibreOffice 成為連接「技術文件世界」與「辦公文件世界」的橋樑——工程師用 Markdown 寫的文件，PM 可以用 Writer 打開來編輯；反過來，Writer 裡排版好的文件也能匯出成 Markdown 放到 GitHub 或技術文件系統上。

支援的 Markdown 方言是 **GitHub Flavored Markdown（GFM）**，這是目前業界最廣泛使用的 Markdown 變體，涵蓋表格、刪除線、任務清單等 GitHub 上常見的語法。

---

## 二、整體架構：三層設計

整個 Markdown 功能的架構可以用「三明治」來理解，分成三層：

```
┌─────────────────────────────────┐
│     LibreOffice 過濾器框架       │  ← 最上層：負責「什麼時候」呼叫
├─────────────────────────────────┤
│     Markdown 匯入/匯出邏輯      │  ← 中間層：負責「怎麼轉換」
├─────────────────────────────────┤
│     md4c 解析引擎（第三方）       │  ← 最底層：負責「怎麼讀懂 Markdown」
└─────────────────────────────────┘
```

**最上層**是 LibreOffice 原有的過濾器框架。Writer 本來就有能力處理各種檔案格式（DOC、DOCX、HTML、ODF 等），每種格式都註冊為一個「過濾器」。Markdown 只是在這個框架中新增了一個過濾器，名稱叫 `"Markdown"`。當使用者開啟或儲存 `.md` 檔案時，框架會自動找到對應的 Markdown 過濾器來處理。

**中間層**是這次新增的核心程式碼，位於 `sw/source/filter/md/` 目錄下，包含約 5 個原始碼檔案。這一層負責兩個方向的轉換：把 Markdown 語法轉成 Writer 的內部文件結構（匯入），以及把 Writer 文件結構轉回 Markdown 文字（匯出）。

**最底層**是一個叫 **md4c** 的開源函式庫。它是一個高效能的 Markdown 解析器，負責把 Markdown 原始文字拆解成結構化的語法元素（標題、段落、粗體、連結等）。LibreOffice 不需要自己從零開始理解 Markdown 語法，只需要接收 md4c 解析好的結果，再轉換成 Writer 能理解的格式即可。

---

## 三、匯入流程：從 Markdown 檔案到 Writer 文件

當使用者在 Writer 中開啟一個 `.md` 檔案時，系統會經過以下步驟：

### 第一步：讀取整份檔案

系統會將整個 Markdown 檔案一次性讀入記憶體。這是因為 md4c 解析器需要看到完整的檔案內容才能正確解析（例如，判斷一個 `*` 是粗體的開始還是清單項目，可能需要參考後面的內容）。

### 第二步：md4c 解析

檔案內容交給 md4c 解析器處理。md4c 會從頭到尾掃過整份文件，每當遇到一個語法元素，就透過「回呼機制」通知 LibreOffice。可以想像成 md4c 是一個翻譯員，它一邊讀 Markdown，一邊對 Writer 喊：

- 「現在進入一個二級標題！」
- 「這裡有一段粗體文字開始了！」
- 「接下來的文字內容是：Hello World」
- 「粗體結束了！」
- 「標題結束了！」

### 第三步：組裝 Writer 文件

LibreOffice 的 Markdown 匯入器（`SwMarkdownParser`）接收這些通知，依序在 Writer 文件中建立對應的內容：

- 收到「進入標題」→ 在文件中建立一個新段落，套用「標題」樣式
- 收到「粗體開始」→ 準備好粗體格式，放進一個「格式堆疊」中等待
- 收到「文字內容」→ 把文字插入文件，同時把堆疊中的格式（粗體）套用上去
- 收到「粗體結束」→ 把粗體格式從堆疊中移除

這個「格式堆疊」的設計很重要，因為 Markdown 允許格式巢狀（例如粗體裡面包斜體），堆疊機制能正確處理這種層層疊加的情況。

### 各種內容元素的處理方式

| Markdown 語法          | Writer 中的對應物                         |
| -------------------- | ------------------------------------ |
| `# 標題` ~ `###### 標題` | 套用「標題 1」~「標題 6」段落樣式                  |
| `- 項目` / `1. 項目`     | 使用 Writer 的編號規則（SwNumRule），支援多層巢狀    |
| `> 引用`               | 套用「HTML 區塊引用」段落樣式                    |
| `` `程式碼` `` / 程式碼區塊  | 套用「預格式化文字」樣式，加上淺灰色背景                 |
| `**粗體**`、`*斜體*`      | 對應的字元格式屬性                            |
| `[連結](URL)`          | Writer 的超連結格式                        |
| `![圖片](URL)`         | 插入圖形物件，自動限制大小（最大約 17.5cm，最小約 1.75cm） |
| `\|表格\|`             | 建立 Writer 表格，保留欄位對齊方式                |
| `- [x] 任務`           | 使用 Writer 的內容控制項（核取方塊）               |
| `[[維基連結]]`           | 轉為 Wikipedia 超連結                     |

### 範本支援

匯入時可以指定一個 Writer 範本（`.ott` 檔案）。系統會先載入範本中定義的樣式，然後再匯入 Markdown 內容。這讓使用者可以用自訂的字型、顏色、間距等風格來呈現 Markdown 文件，而不是只能用預設樣式。

---

## 四、匯出流程：從 Writer 文件到 Markdown 檔案

匯出是匯入的反向過程，但實作上更複雜，因為 Writer 文件的格式遠比 Markdown 豐富，匯出器需要判斷哪些格式能轉換、哪些只能捨棄。

### 第一步：收集資訊

匯出器首先掃描整份文件，收集所有「錨定物件」（主要是圖片）的位置資訊。這是因為 Writer 中的圖片是獨立的浮動物件，需要先知道它們分別錨定在哪個段落的哪個位置，才能在匯出時把它們放回正確的位置。

### 第二步：逐節點走訪

Writer 的文件內部是一棵「節點樹」，匯出器會從頭到尾走訪每個節點：

- 遇到**文字節點**：分析段落樣式，判斷這是標題、清單項目、引用還是一般段落，然後輸出對應的 Markdown 語法。
- 遇到**表格節點**：記錄表格結構（幾列幾行、各欄對齊方式），在適當位置輸出 Markdown 表格語法。

### 第三步：字元級格式追蹤（狀態機）

匯出器最精密的部分是處理字元格式。它維護一個「格式狀態機」，同時追蹤十幾種格式維度的變化：

- 粗體是否開啟？
- 斜體是否開啟？
- 程式碼字型是否開啟？
- 刪除線是否開啟？
- 目前在哪個超連結裡？
- 是否有修訂追蹤的插入/刪除標記？

匯出器逐字元掃過段落，在每個位置檢查格式是否發生變化。如果某個格式從「關閉」變成「開啟」，就輸出對應的 Markdown 開啟標記（如 `**`）；反之則輸出關閉標記。這種逐字元的精確追蹤確保了格式標記能出現在正確的位置。

### 特殊處理

- **修訂追蹤（Track Changes）**：Writer 的修訂標記會匯出為 HTML 標籤 `<ins>` 和 `<del>`，並附帶作者名稱和時間戳。這是因為標準 Markdown 沒有修訂追蹤語法，但 HTML 標籤在 Markdown 中是合法的，且語意明確。

- **嵌入式圖片**：如果圖片是嵌入在文件中（而非外部連結），匯出器會將圖片轉為 Base64 編碼的 data URL，確保 Markdown 檔案可以獨立攜帶圖片資料。

- **特殊字元跳脫**：Markdown 中有許多特殊字元（`*`、`_`、`[`、`#` 等），匯出器會自動在一般文字中的這些字元前面加上反斜線 `\`，避免被誤判為 Markdown 語法。

---

## 五、程式碼結構

所有 Markdown 相關的程式碼集中在一個目錄下，共 5 個檔案，職責劃分清晰：

```
sw/source/filter/md/
├── swmd.hxx / swmd.cxx      匯入器主體（SwMarkdownParser 類別）
├── wrtmd.hxx / wrtmd.cxx    匯出器主體（SwMDWriter 類別）
├── mdcallbcks.cxx            md4c 回呼函式（接收解析事件的介面）
├── mdnum.hxx / mdnum.cxx    清單編號管理
└── mdtab.cxx                 表格匯入處理
```

此外，相關的程式碼還分布在：

- `external/md4c/`：第三方 md4c 解析函式庫
- `sw/source/filter/basflt/fltini.cxx`：過濾器註冊點（將 Markdown 過濾器接入 Writer 的過濾器框架）
- `sw/qa/filter/md/`：自動化測試（涵蓋標題、清單、表格、圖片、修訂追蹤等場景）

---

## 六、關鍵設計決策

### 1. 選用 md4c 作為解析引擎

團隊選擇使用現有的開源解析器 md4c，而非自行開發。md4c 是一個以 C 語言撰寫的高效能解析器，完整支援 CommonMark 標準和 GitHub 擴充語法。這個決策降低了開發成本，也確保了對 Markdown 規範的完整相容性。LibreOffice 只需要對 md4c 做極小的修改（一個 const 正確性的補丁）。

### 2. 採用回呼模式（Callback Pattern）

匯入採用「事件驅動」的回呼模式：md4c 解析時會觸發事件（進入標題、離開標題、遇到文字等），匯入器只需實作這些事件的處理邏輯。這種模式讓解析邏輯和文件建構邏輯完全分離，程式碼結構清晰。

### 3. 複用 Writer 既有的段落樣式

匯入器不建立新樣式，而是直接使用 Writer 內建的樣式池（如「標題 1」、「預格式化文字」、「HTML 區塊引用」等）。這確保匯入的 Markdown 文件和手動在 Writer 中建立的文件使用完全相同的樣式體系，使用者可以在匯入後自由修改樣式。

### 4. 圖片大小限制

匯入時對圖片設定了合理的大小範圍（最小約 1.75cm，最大約 17.5cm），避免過大的圖片撐破版面，也避免過小的圖片看不見。系統會在限制範圍內保持圖片的原始長寬比。

### 5. 匯出時保留修訂追蹤資訊

匯出器將修訂追蹤轉為 HTML 的 `<ins>` / `<del>` 標籤，而非直接丟棄。這是一個務實的折衷方案：雖然 Markdown 原生不支援修訂追蹤，但透過 HTML 標籤可以在不破壞 Markdown 相容性的前提下保留這些資訊。

---

## 七、功能邊界與限制

以下是目前 Markdown 支援的已知限制，有助於設定使用者期望：

| 面向                       | 支援情況                    |
| ------------------------ | ----------------------- |
| 基本格式（粗體、斜體、刪除線）          | 完整支援雙向轉換                |
| 標題（1-6 級）                | 完整支援                    |
| 有序 / 無序清單（含巢狀）           | 完整支援                    |
| 表格（含欄位對齊）                | 完整支援                    |
| 程式碼區塊與行內程式碼              | 完整支援                    |
| 圖片（外部連結與嵌入）              | 完整支援                    |
| 超連結                      | 完整支援                    |
| 任務清單（核取方塊）               | 完整支援                    |
| 區塊引用                     | 完整支援                    |
| 維基連結 `[[...]]`           | 匯入支援（轉為 Wikipedia 連結）   |
| 修訂追蹤                     | 匯出為 HTML 標籤             |
| Writer 專有格式（頁首頁尾、分頁、欄位等） | 匯出時會被忽略                 |
| 數學公式（LaTeX）              | md4c 可解析，但 Writer 端處理有限 |
| 巢狀表格                     | 不支援（Markdown 規範本身不支援）   |

---

## 八、測試覆蓋

自動化測試位於 `sw/qa/filter/md/`，使用 CppUnit 測試框架，覆蓋以下場景：

- 標題匯入（層級和大綱層級的正確性）
- 清單匯入（有序、無序、巢狀層級）
- 表格匯入（結構完整性、欄位對齊）
- 基本元素匯出（標題、粗體、斜體、連結、特殊字元跳脫）
- 程式碼格式匯出
- 清單匯出（層級、編號、項目符號）
- 修訂追蹤匯出
- 表格中的圖片處理

測試同時涵蓋匯入和匯出方向，確保雙向轉換的正確性。

---

## 附錄 A：超越檔案匯入匯出——剪貼簿整合

Markdown 過濾器的觸發範圍不限於「開啟檔案」和「另存新檔」。事實上，它已經深入整合到 Writer 的剪貼簿系統中，在日常編輯操作裡靜默運作。

### 複製時：自動提供 Markdown 格式

當使用者在 Writer 中複製一段內容（Ctrl+C），系統會同時準備多種剪貼簿格式供接收端選擇。除了傳統的 RTF、HTML、純文字之外，Writer 現在也會透過 `SwMDWriter` 將選取的內容即時轉為 Markdown，放入剪貼簿中（格式識別碼為 `SotClipboardFormatId::MARKDOWN`）。

這意味著：如果使用者在 Writer 中複製一段帶有標題、粗體、清單的內容，然後貼到一個 Markdown 編輯器（如 VS Code、Obsidian），接收端可以直接取得格式正確的 Markdown 文字，而不是失去所有格式的純文字。

相關程式碼位於 `sw/source/uibase/dochdl/swdtflvr.cxx` 的 `SwTransferable::GetData()` 和 `SwTransferable::WriteObject()` 中。

### 貼上時：自動偵測 Markdown 內容

這是對使用者來說最「無感」但最實用的整合。當使用者按下 Ctrl+V 貼上純文字時，系統會在背後呼叫 `comphelper::IsMarkdownData()` 分析剪貼簿中的純文字內容。這個偵測函式會檢查文字的前 2000 個字元，尋找 Markdown 的特徵模式：

- 標題語法（`# Title`）
- 連結語法（`[text](url)`）
- 程式碼區塊（`` ``` ``）
- 粗體/斜體標記（`**text**`、`*text*`）
- 刪除線（`~~text~~`）
- 分隔線（`---`、`***`）
- 區塊引用（`> text`）
- 表格語法（`| col |`）

如果偵測結果判定為 Markdown，系統會自動將純文字交給 `MarkdownReader`（與開啟 `.md` 檔案相同的匯入器）進行解析，將格式化的內容插入到游標位置。使用者不需要做任何額外操作——從一個 Markdown 文件或網頁上複製內容，貼到 Writer 裡就會自動帶有格式。

此偵測邏輯位於 `swdtflvr.cxx` 的 `SwTransferable::PasteFileContent()` 中，相關的偵測演算法實作在 `comphelper/source/misc/markdown.cxx`。

### 選擇性貼上：手動選擇 Markdown 格式

使用者也可以透過「選擇性貼上」（Ctrl+Shift+V）手動選擇以 Markdown 格式貼上。當剪貼簿中包含 Markdown 格式的資料時，貼上對話框會列出「Markdown」作為可選格式之一。

### Calc 的特殊處理

在 Calc（試算表）中貼上時，如果系統偵測到剪貼簿內容是 Markdown 格式的表格，會跳出對話框讓使用者選擇：以「Markdown」格式貼上（系統會將 Markdown 表格結構轉為 CSV，分配到對應的儲存格中），或是以「純文字」格式貼上。相關邏輯位於 `sc/source/ui/view/viewfun5.cxx`。

---

## 附錄 B：未來擴充方向——Markdown 輸入對話框

### 構想

在 Writer 的選單或工具列中新增一個「插入 Markdown」入口，開啟一個對話框讓使用者直接輸入或貼上 Markdown 原始文字，按下確認後將轉換後的格式化內容插入到文件的目前游標位置。

### 為什麼可行性高

這個方案的核心優勢在於：**所有轉換邏輯都已經存在且經過驗證**。

Markdown 匯入器（`SwMarkdownParser`）在設計時就支援兩種模式：「建立新文件」和「插入到既有文件」。後者正是貼上功能已經在使用的路徑。差別只在於內容的來源——貼上是從剪貼簿取得文字，對話框則是從使用者的輸入框取得文字。

具體來說，匯入器的建構函式接受一個 `bReadNewDoc` 參數：
- `true`：建立全新的空白文件再填入內容（用於開啟 `.md` 檔案）
- `false`：在現有文件的游標位置插入內容（用於貼上、也可用於對話框）

### 實作路徑概述

實作這個功能大致需要以下步驟：

1. **建立 UI 對話框**：在 `sw/source/uibase/` 下新增對話框，包含一個多行文字輸入區域和確認/取消按鈕。LibreOffice 的對話框使用 XML 描述檔（位於 `/uiconfig/` 目錄）定義介面佈局，搭配 C++ 的對話框控制器類別處理互動邏輯。

2. **註冊選單項目**：在 Writer 的「插入」選單中新增「Markdown...」項目，綁定到一個新的命令（dispatch command），觸發時開啟上述對話框。

3. **串接轉換邏輯**：使用者按下確認時，將輸入框的文字內容包裝成記憶體串流（`SvMemoryStream`），然後呼叫現有的 `MarkdownReader::Read()` 方法，以「插入模式」將轉換後的內容注入到文件的目前游標位置。

4. **參考現有實作**：整個串接方式可以直接參考剪貼簿貼上的程式碼路徑。在 `swdtflvr.cxx` 的 `PasteFileContent()` 函式中，系統已經在做完全相同的事情：將純文字內容餵給 `MarkdownReader`，以插入模式寫入文件。對話框方案只是把「文字來源」從剪貼簿換成對話框的輸入區域。

### 與現有功能的關係

這個對話框方案與現有的「貼上自動偵測」功能互補而非重複：

- **貼上自動偵測**是「無感」的——它在背後自動判斷，使用者不需要知道 Markdown 的存在。但自動偵測可能誤判（普通文字被誤認為 Markdown），且使用者無法控制是否啟用轉換。
- **對話框**是「有意識」的——使用者明確選擇要插入 Markdown，不存在誤判問題。適合使用者手上有一段 Markdown 原始碼，想要精確控制插入行為的場景。
